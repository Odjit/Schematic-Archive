{
  "name": "schematic-archive",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "The Schematic Archive: a community gallery of V Rising builds for the KindredSchematics mod.",
  "scripts": {
    "dev": "pnpm run build-render-prefabs && pnpm run build-index && astro dev",
    "build": "pnpm run build-render-prefabs && pnpm run build-index && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "build-index": "node scripts/build-index.mjs",
    "build-render-prefabs": "node scripts/build-render-prefabs.mjs",
    "render-floorplan": "node scripts/render-floorplan.mjs",
    "validate": "node scripts/validate-manifests.mjs",
    "og": "node scripts/build-og.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@astrojs/preact": "^4.0.0",
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.0.0",
    "minisearch": "^7.1.0",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1",
    "sharp": "^0.34.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.12.0"
}
